name: Notify website of release
on:
  push:
    branches: [ main ]
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger website rebuild
        env:
          DEPLOY_HOOK: ${{ secrets.LEGANDYWEB_DEPLOY_HOOK }}
        run: curl -fsS -X POST "$DEPLOY_HOOK"